task_id: aitool-lightweight-cleanup
current_table: lightweight_cleanup
stage: 轻量化重构
status: in_progress
task_type: deliverable
priority_level: 主线必解
actor: aitool-master-controller
allow_write:
  - AGENTS.md
  - docs/
  - .agent/
  - src/
  - tests/
  - run_desktop_tool.py
next_step: >
  轻量化清理已完成：移除已删除文件的残余测试、修复 write_scope_gate 硬依赖、
  删除冲突的 build_exe.py、清理过时引用。验证测试与治理检查已通过，下一步：创建清理提交。
override:
  enabled: false
history_note: >
  2026-07-12 轻量化重构：删除母仓特定基建（send_msg/controller-registry/practice-registry/
  skill-governance/self-evolution-architecture）、无关脚本（website_probe）、过期文档。
  .agent/ 从 26 文件降到 13，handbook 从 406 行降到 43 行。
  保留核心三件套 + 护栏 + 打包脚本。
